# 开发环境配置
spring:
  datasource:
    url: jdbc:h2:file:./data/novelreader;DB_CLOSE_DELAY=-1

  jpa:
    show-sql: true

  h2:
    console:
      enabled: true

logging:
  level:
    com.novelreader: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
